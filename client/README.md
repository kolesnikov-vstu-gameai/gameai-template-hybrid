# Unity-клиент

1. `git lfs install`
2. Создайте Unity-проект (2022.3 LTS) в папке `client/UnityProject/`.
3. Скопируйте `client/starter/*` в `UnityProject/Assets/_Project/Scripts/`.
4. Адрес сервера задаётся в `ServerConfig` (по умолчанию `http://localhost:8000`).
